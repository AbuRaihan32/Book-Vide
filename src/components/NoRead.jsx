
const NoRead = ({text}) => {
    return (
        <div className="min-h-[calc(100vh-700px)] flex justify-center items-center">
            <h1 className="text-4xl font-bold">{text}</h1>
        </div>
    );
};

export default NoRead;